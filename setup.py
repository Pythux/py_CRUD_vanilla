from setuptools import setup

setup(
    name='database',
    version='1.0',
    description='database tools to link models to data_access',
    author='Pythux',
    # author_email='',
    packages=['database'],  # same as name
    # install_requires=[],  # external packages as dependencies
)
